PIPELINE


diff --git a/core/pipeline.py b/core/pipeline.py
index 09d4ce7245a123ffb79ef2a0ea575aa0a0351535..971d877979b887feb49755a685aa2cb27bfd170b 100644
--- a/core/pipeline.py
+++ b/core/pipeline.py
@@ -6,50 +6,52 @@ from connectors.pyhamilton_connector import create_simulated_interface
 from connectors.pyhamilton_execution_research import build_pyhamilton_dry_run_plan, summarize_pyhamilton_plan, validate_pyhamilton_dry_run_plan
 from connectors.venus_execution_research import build_venus_execution_readiness_summary, detect_run_control_candidates, explain_required_manual_steps
 from core.runtime_evidence_collectors import collect_all_runtime_evidence
 from core.operator_attestation import load_operator_attestation, summarize_operator_attestation
 from core.runtime_session import build_runtime_session_artifacts, create_runtime_research_session, summarize_runtime_session
 from core.safety_gates import evaluate_execution_readiness
 from core.runtime_environment import detect_runtime_environment
 from core.runtime_modes import RUNTIME_MODE_PYHAMILTON_SIMULATE, RUNTIME_MODE_VENUS_RUN_CONTROL_SIMULATION
 
 from analyzers.runtime_simulation_analyzer import analyze_runtime_simulation
 from core.bounded_sweep import generate_bounded_scenarios
 from core.config import DevPalConfig
 from core.orchestrator import build_full_method_model, run_full_analysis
 from models.method_models import MethodModel, StressScenario
 
 
 def run_pipeline(config: DevPalConfig) -> MethodModel:
     config.resolve_paths(); config.discover_trace_files(); config.ensure_output_directory(); config.validate()
     model = build_full_method_model(selected_method_path=config.selected_method_path, project_dir=config.project_dir, layout_paths=config.layout_paths, document_paths=config.document_paths, trace_paths=config.trace_paths if config.enable_trace_analysis else [])
     model.runtime_metadata.update({"requested_runtime_mode": config.runtime_mode, "local_runtime_check_requested": config.local_runtime_check, "pyhamilton_sim_allowed": config.allow_pyhamilton_sim, "execution_performed": False, "venus_launch_performed": False, "method_execution_performed": False, "hardware_execution_occurred": False})
 
     runtime_data = detect_runtime_environment(config.trace_paths if config.enable_trace_analysis else [])
     model.runtime_metadata["runtime_environment"] = runtime_data["environment"].__dict__
     model.runtime_metadata["runtime_capabilities"] = [cap.__dict__ for cap in runtime_data["capabilities"]]
     model.runtime_metadata["runtime_validation_attempts"] = []
+    model.runtime_metadata.setdefault("sequence_candidates_found_but_ambiguous", False)
+    model.runtime_metadata.setdefault("simulation_trace_sequence_mapping_readiness", {"ready": False, "reason": "metadata_only_no_simulation"})
 
     if config.local_runtime_check and config.runtime_mode == RUNTIME_MODE_PYHAMILTON_SIMULATE:
         if config.allow_pyhamilton_sim:
             sim_result = create_simulated_interface()
             model.runtime_metadata["pyhamilton_sim_interface_created"] = bool(sim_result["simulated_interface_created"])
             model.runtime_metadata["runtime_validation_attempts"].append({"mode": RUNTIME_MODE_PYHAMILTON_SIMULATE, "attempted": True, "result": {k: v for k, v in sim_result.items() if k != "interface"}})
             for lim in sim_result.get("limitations", []):
                 model.add_issue(severity="minor", category="runtime-limitation", message=lim, recommendation="Validate local Windows VENUS/PyHamilton runtime setup.")
         else:
             model.runtime_metadata["pyhamilton_sim_interface_created"] = False
             model.add_issue(severity="minor", category="runtime-limitation", message="pyhamilton_simulate requested without --allow-pyhamilton-sim; simulate interface creation was not attempted.", recommendation="Re-run with --allow-pyhamilton-sim for guarded simulate=True interface check.")
 
     if config.local_runtime_check and config.runtime_mode == RUNTIME_MODE_VENUS_RUN_CONTROL_SIMULATION:
         model.runtime_metadata["venus_readiness_checked"] = True
         model.add_issue(severity="info", category="runtime-readiness", message="VENUS run-control readiness checked only; live VENUS launch/execution is not implemented in Phase 1.", recommendation="Use this evidence for local validation planning; keep execution outside DevPal Phase 1 scope.")
 
     sweep = generate_bounded_scenarios(model)
     model.runtime_metadata["sweep_metadata"] = sweep.metadata
     model.runtime_metadata["sweep_boundaries"] = [boundary.__dict__ for boundary in sweep.boundaries]
     model.runtime_metadata["generated_stress_scenarios"] = [scenario.__dict__ for scenario in sweep.scenarios]
     linkage_payload = _build_linkage_payload(model, preview_limit=50)
     generated = []
     for item in sweep.scenarios:
         scenario_linkage = dict(item.linkage)
         scenario_linkage.update({




************* 
REPORT BUILD



diff --git a/core/report_builder.py b/core/report_builder.py
index 1dc26db1e2083bacd3b97d68dfbb4bb88d277d5f..002b4f99b655217c912908cd7d7c914e24e8bf62 100644
--- a/core/report_builder.py
+++ b/core/report_builder.py
@@ -19,51 +19,53 @@ def build_full_report(model: MethodModel)->Dict[str,Any]:
     reg=EvidenceRegistry(); findings=[]
     for issue in model.validation_issues:
         findings.append({"finding_id":_finding_id(issue),"title":issue.message,"message":issue.message,"severity":issue.severity,"category":issue.category,"status":_status(issue),"confidence":issue.confidence or "deterministic","file_path":issue.file_path,"line_number":issue.line_number,"step_id":issue.step_id,"evidence_ids":sorted(set(reg.register_many(issue.evidence))),"recommendation":issue.recommendation,"related_objects":sorted(set(issue.related_objects)),"analyzer":issue.analyzer})
     findings.sort(key=lambda x:(_SEVERITY_RANK.get((x.get("severity") or "major").lower(),9),x.get("category") or "",x.get("file_path") or "",x.get("line_number") or 0,x.get("finding_id") or ""))
     runtime_env=model.runtime_metadata.get("runtime_environment",{}); runtime_caps=model.runtime_metadata.get("runtime_capabilities",[])
     runtime_modes_available=[]
     for capability in runtime_caps:
         if not capability.get("available",False):
             continue
         mode_name=capability.get("mode") or capability.get("name")
         if mode_name:
             runtime_modes_available.append(mode_name)
     phase2=model.runtime_metadata.get("phase2_runtime_research",{})
     report={
       "summary":{"issues_total":len(findings),"positives_total":len(model.positive_findings),"scenario_count":len(model.stress_test_plan.scenarios)},
       "input_files":{"method":model.files.selected_method_path,"layouts":model.files.uploaded_layout_paths,"documents":model.files.uploaded_requirement_paths,"traces":model.files.trc_paths},
       "parsed_data_overview":{"steps":len(model.steps),"sequences":len(model.deck.sequences),"labware":len(model.deck.labware),"transfer_pairs":len(model.transfer_pairs)},
       "parsed_file_summary":{"uploaded_layout_count":len(model.files.uploaded_layout_paths),"requirement_document_count":len(model.files.uploaded_requirement_paths),"trace_file_count":len(model.files.trc_paths)},
       "static_analysis_findings":findings,
       "issues":{"all":findings},"issue_summary":{"total":len(findings),"severity_counts":{k:len([f for f in findings if f['severity']==k]) for k in ['critical','major','minor','info','pass']}},
       "runtime_environment":{"requested_runtime_mode":model.runtime_metadata.get("requested_runtime_mode","auto"),"runtime_mode":runtime_env.get("runtime_mode","static_only"),"local_runtime_check_requested":model.runtime_metadata.get("local_runtime_check_requested",False),"pyhamilton_sim_allowed":model.runtime_metadata.get("pyhamilton_sim_allowed",False),"runtime_modes_available":runtime_modes_available,"execution_performed":False,"hardware_execution_occurred":False,"venus_launched":False,"method_execution_occurred":False,"trace_replay_used":len(model.trace_runs)>0,"pyhamilton_sim_interface_created":model.runtime_metadata.get("pyhamilton_sim_interface_created",False),"venus_readiness_checked":model.runtime_metadata.get("venus_readiness_checked",False),"runtime_validation_attempts":model.runtime_metadata.get("runtime_validation_attempts",[]),"no_hardware_execution_statement":"No live hardware execution was launched by DevPal in this run."},
       "runtime_capabilities":runtime_caps,
       "bounded_sweep_summary":model.runtime_metadata.get("sweep_metadata",{}),
       "transfer_sequence_volume_linkage_summary":{"mappings":len(model.transfer_mappings),"pairs":len(model.transfer_pairs)},
       "trace_replay_correlation_summary":{"trace_runs":len(model.trace_runs)},
-      "documentation_crosscheck_summary":{"requirements":len(model.documents.requirements)},"trace":[{"path":t.path,"status":t.run_status,"events":len(t.events),"errors":len(t.errors)} for t in model.trace_runs],
+      "simulation_trace_sequence_mapping_readiness": model.runtime_metadata.get("simulation_trace_sequence_mapping_readiness", {}),
+      "sequence_candidates_found_but_ambiguous": bool(model.runtime_metadata.get("sequence_candidates_found_but_ambiguous", False)),
+      "documentation_crosscheck_summary":{"requirements":len(model.documents.structured_requirements or model.documents.requirements)},"documentation_extraction_summary":model.documents.documentation_extraction_summary,"structured_requirements":[{"requirement_id":r.requirement_id,"text":r.text,"source_file":r.source_file} for r in (model.documents.structured_requirements or model.documents.requirements)],"documentation_rows":model.documents.documentation_rows,"requirement_extraction_limitations":model.documents.requirement_extraction_limitations,"trace":[{"path":t.path,"status":t.run_status,"events":len(t.events),"errors":len(t.errors)} for t in model.trace_runs],
       "limitations":[{"statement":f["message"],"finding_id":f["finding_id"]} for f in findings if f["status"] in {"limitation","unverifiable","missing"}],
       "warnings":[f for f in findings if f["severity"] in {"minor","major","critical"}],"positives":list(model.positive_findings),
       "next_recommended_actions":sorted({f["recommendation"] for f in findings if f.get("recommendation")}),"evidence_registry":reg.as_list(),
       "coverage_limits":["Static analysis and trace replay only in this environment when runtime capabilities are unavailable."],
       "phase2_runtime_research": phase2,
       "runtime_evidence_collectors": phase2.get("runtime_evidence_collectors", {}),
       "operator_attestation": phase2.get("operator_attestation", {}),
       "runtime_session": phase2.get("runtime_session", {}),
       "generated_artifacts": phase2.get("generated_artifacts", []),
       "artifact_hashes": phase2.get("artifact_hashes", {}),
       "rollback_playbook_reference": phase2.get("rollback_playbook_reference", "docs/RUNTIME_ABORT_AND_ROLLBACK_PLAYBOOK.md"),
       "chain_of_custody": phase2.get("chain_of_custody", {}),
       "safety_gates": phase2.get("execution_readiness", {}).get("gates", []),
       "dry_run_plan_summary": phase2.get("dry_run_plan_summary", {}),
       "execution_readiness": phase2.get("execution_readiness", {}),
       "blocked_execution_reason": phase2.get("blocked_execution_reason", "Execution remains blocked by default."),
       "execution_performed": False,
       "hardware_execution_occurred": False,
       "venus_launched": False,
       "method_execution_occurred": False,
       "no_live_execution_statement": phase2.get("no_live_execution_statement", "No live execution performed."),
     }
     report["limitations"].extend([
       {"statement":".med parsing is stub-only in Phase 1 (presence + references; no GUI/OCR parsing)."},
       {"statement":"No OCR/AWS Textract/image parsing is performed."},





**********
COREEEE

diff --git a/docs/CODEX_PASS_LOG.md b/docs/CODEX_PASS_LOG.md
index 1bb8a728a4ac84714bb3ef33a9ff8261a38dd890..d79b948fb4a51d5f68dc151fa4516625bdb0b2e5 100644
--- a/docs/CODEX_PASS_LOG.md
+++ b/docs/CODEX_PASS_LOG.md
@@ -154,25 +154,33 @@ Implemented local-gated runtime validation hardening (safe readiness checks only
 - No OCR/Textract/.med GUI parsing added.
 - Runtime limitation signaling remains explicit (no fake success behavior).
 
 
 ## Pass 9 — Final Local PC Verification Workflow (2026-05-14)
 - Added `docs/LOCAL_PC_VERIFICATION.md` with step-by-step Windows local verification checklist, expected safety booleans, and evidence collection guidance.
 - Added `scripts/local_pc_verification.py` as a stdlib-only, Python 3.9-compatible safe runner for pytest/CLI/readiness checks.
 - Runner captures exit codes and output, writes `reports/local_pc_verification_summary.json`, and inspects generated report JSON safety booleans (`execution_performed`, `hardware_execution_occurred`, `venus_launched`, `method_execution_occurred`) expecting all false.
 - Added `tests/test_local_pc_verification_script.py` covering safe command construction, no VENUS executable launch terms, summary generation, nonzero exit handling, safety-boolean extraction, and limitation-vs-failure behavior.
 - No live execution added: no VENUS launch, no hardware execution, no method execution, no fake runtime success semantics.
 
 ## Phase 2 pass 1
 - Implemented safety gates, readiness reporting, and dry-run planning scaffolding only.
 - Explicitly blocked live execution paths.
 
 - Added Pass 2 runtime evidence collectors, attestation validation, rollback playbook, runtime session artifact hashing, and chain-of-custody reporting. Execution remains blocked.
 
 ## Domain Knowledge Documentation Pass
 
 - Added `docs/HAMILTON_VENUS_DOMAIN_KNOWLEDGE.md` as repo-local Hamilton/VENUS/Python/PyHamilton doctrine.
 - Documented VENUS file roles, parsing doctrine, sequence/transfer/trace/plate marker concepts, PyHamilton/PyLabRobot posture, and safety boundaries.
 - Added `AGENTS.md` pointer so future Codex passes read the domain doc before parser/runtime work.
 - Documentation only; no parser/analyzer/runtime behavior changed.
 - No live execution added.
 
+
+## Pass 6 — Documentation extraction + reporting surfaces + sequence mapping readiness metadata (2026-05-18)
+- Expanded documentation parsing for `.docx` and `.pdf` foundations with structured requirement extraction paths and explicit limitations when no accessible DOCX text is detected.
+- Added `MethodDocumentSet` structured fields: `structured_requirements`, `documentation_rows`, `documentation_extraction_summary`, and `requirement_extraction_limitations`.
+- Added boolean-safe `_merge_summary` behavior (bool OR before integer sum) for extraction summary aggregation.
+- Updated report payload to expose Pass 6 documentation fields and crosscheck requirement counting via `structured_requirements` fallback semantics.
+- Added metadata-only sequence mapping readiness surfaces (`simulation_trace_sequence_mapping_readiness`, `sequence_candidates_found_but_ambiguous`) without introducing live execution behavior.
+- Added focused tests for summary merge typing, DOCX metadata propagation, and safety boolean persistence.



*********

METHOD



diff --git a/models/method_models.py b/models/method_models.py
index 2ebd7516d60498177b31b68a2fd893746418dc7d..adf6252cd7d6fea6ddf4f7bdc694697332a86d4c 100644
--- a/models/method_models.py
+++ b/models/method_models.py
@@ -605,50 +605,54 @@ class RequirementRecord:
     expected_extensions: List[str] = field(default_factory=list)
 
     expected_sample_min: Optional[int] = None
     expected_sample_max: Optional[int] = None
     expected_plate_min: Optional[int] = None
     expected_plate_max: Optional[int] = None
 
     expected_volumes_ul: List[float] = field(default_factory=list)
     expected_tip_types: List[str] = field(default_factory=list)
     expected_labware: List[str] = field(default_factory=list)
     expected_sequences: List[str] = field(default_factory=list)
 
     requires_error_handling: bool = False
     requires_recovery: bool = False
     requires_abort_after_attempts: Optional[int] = None
     requires_tracking_order: bool = False
     requires_output_copy: bool = False
     requires_initialize: bool = False
     raw_text: str = ""
 
 
 @dataclass
 class MethodDocumentSet:
     documents: List[str] = field(default_factory=list)
     requirements: List[RequirementRecord] = field(default_factory=list)
+    structured_requirements: List[RequirementRecord] = field(default_factory=list)
+    documentation_rows: List[Dict[str, str]] = field(default_factory=list)
+    documentation_extraction_summary: Dict[str, Any] = field(default_factory=dict)
+    requirement_extraction_limitations: List[str] = field(default_factory=list)
     parse_warnings: List[str] = field(default_factory=list)
 
 
 @dataclass
 class FileOperation:
     operation_type: str = FILE_ROLE_UNKNOWN
     path_expression: str = ""
     resolved_path: Optional[str] = None
     extension: Optional[str] = None
     source_file: str = ""
     line_number: Optional[int] = None
     raw_text: str = ""
 
 
 @dataclass
 class LookupTableUse:
     table_name: str
     path_expression: str = ""
     resolved_path: Optional[str] = None
     source_file: str = ""
     line_number: Optional[int] = None
     function_call: Optional[str] = None
     raw_text: str = ""
 
 


*****
DOCCCC

diff --git a/parsers/documentation_parser.py b/parsers/documentation_parser.py
index 1c42a1989a6a05eeba1bc84a7be76e3d9379eb38..79cd2a606affbfe7d2304e39c124351712179f2b 100644
--- a/parsers/documentation_parser.py
+++ b/parsers/documentation_parser.py
@@ -1,143 +1,237 @@
 from __future__ import annotations
 
 import os
 import re
-from typing import Dict, List, Optional
+from typing import Any, Dict, List, Optional
 
 from models.method_models import (
     MethodDocumentSet,
     MethodModel,
     RequirementRecord,
     ValidationIssue,
 )
 from parsers.utils import (
     clean_raw_token,
     extract_file_references,
     extract_max_sample_requirement,
     extract_volume_requirements,
     normalize_hamilton_text,
     read_text_file,
     text_contains_any,
 )
 
 
 COMMENT_BLOCK_PATTERN = re.compile(r"/\*(?P<body>.*?)\*/", re.DOTALL)
 LINE_COMMENT_PATTERN = re.compile(r"^\s*//(?P<body>.*)$", re.MULTILINE)
 
 REQUIREMENT_ID_PATTERN = re.compile(
-    r"(?i)\b(?:UR|REQ|Requirement|MP|SOP)\s*[-_#:]?\s*([A-Za-z0-9_.\-]+)"
+    r"(?i)\b((?:UR|REQ|RQ|MP|SOP)\s*[-_ ]?\d{1,6}|\d+(?:\.\d+){2,}|Requirement\s*[-_#:]?\s*[A-Za-z0-9_.\-]+)\b"
 )
 
 STRUCTURED_REQUIREMENT_LINE_PATTERN = re.compile(
-    r"^\s*(?P<rid>(?:UR|REQ|MP|SOP)\s*[-_]?\s*\d{1,6}|Requirement\s*[-_#:]?\s*[A-Za-z0-9_.\-]+)\s*[:\-]\s*(?P<body>.*)$",
+    r"^\s*(?P<rid>(?:UR|REQ|RQ|MP|SOP)\s*[-_ ]?\d{1,6}|\d+(?:\.\d+){2,}|Requirement\s*[-_#:]?\s*[A-Za-z0-9_.\-]+)\s*[:\-]\s*(?P<body>.*)$",
     re.IGNORECASE,
 )
 
 SECTION_PATTERN = re.compile(
     r"(?i)\b(?:section|heading|title)\s*[:\-]\s*([A-Za-z0-9 _\-.]+)"
 )
 
 TIP_PATTERN = re.compile(
     r"(?i)\b(1000ul|300ul|300ul_slim|50ul)\b"
 )
 
 SEQUENCE_PATTERN = re.compile(
     r"(?i)\b([A-Za-z0-9_.\-]*(?:seq|sequence|sample|buffer|qc|std|tip)[A-Za-z0-9_.\-]*)\b"
 )
 
 LABWARE_PATTERN = re.compile(
     r"(?i)\b([A-Za-z0-9_.\-]*(?:plate|rack|tube|carrier|reservoir|dwp|mte)[A-Za-z0-9_.\-]*)\b"
 )
 
 
 class DocumentationParserError(Exception):
     pass
 
 
 def parse_documentation_file(path: str, model: Optional[MethodModel] = None) -> MethodDocumentSet:
     if not path:
         raise DocumentationParserError("No documentation path provided.")
 
     if not os.path.isfile(path):
         raise DocumentationParserError("Documentation file not found: {0}".format(path))
 
     try:
         text = _read_document_text(path)
     except (ImportError, OSError, ValueError) as exc:
         raise DocumentationParserError(
             "Documentation file could not be parsed deterministically: {0}: {1}".format(path, exc)
         )
     requirements = _build_requirements_by_extension(path, text)
+    doc_rows=[]
+    extraction_summary={}
+    extraction_limitations=[]
+    if os.path.splitext(path)[1].lower()==".docx":
+        extracted=_extract_docx_rows(path)
+        if extracted.get("requirements"):
+            requirements=extracted["requirements"]
+        doc_rows=extracted.get("rows",[])
+        extraction_summary=extracted.get("summary",{})
+        extraction_limitations=extracted.get("limitations",[])
 
     document_set = MethodDocumentSet(
         documents=[path],
         requirements=requirements,
+        structured_requirements=list(requirements),
+        documentation_rows=doc_rows,
+        documentation_extraction_summary=extraction_summary,
+        requirement_extraction_limitations=extraction_limitations,
         parse_warnings=[],
     )
 
     if model is not None:
         model.documents.documents.append(path)
         model.documents.requirements.extend(requirements)
+        model.documents.structured_requirements.extend(requirements)
+        model.documents.documentation_rows.extend(doc_rows)
+        model.documents.documentation_extraction_summary = _merge_summary(model.documents.documentation_extraction_summary, extraction_summary)
+        model.documents.requirement_extraction_limitations.extend(extraction_limitations)
 
         if not requirements:
             model.validation_issues.append(
                 ValidationIssue(
                     severity="minor",
                     category="documentation",
                     message="No structured requirements parsed from documentation file.",
                     related_objects=[path],
                 )
             )
 
     return document_set
 
 
 def parse_multiple_documents(paths: List[str], model: Optional[MethodModel] = None) -> MethodDocumentSet:
     combined = MethodDocumentSet()
 
     for path in paths:
         try:
             result = parse_documentation_file(path, model=None)
             combined.documents.extend(result.documents)
             combined.requirements.extend(result.requirements)
+            combined.structured_requirements.extend(result.structured_requirements)
+            combined.documentation_rows.extend(result.documentation_rows)
+            combined.documentation_extraction_summary = _merge_summary(combined.documentation_extraction_summary, result.documentation_extraction_summary)
+            combined.requirement_extraction_limitations.extend(result.requirement_extraction_limitations)
         except (DocumentationParserError, FileNotFoundError, ValueError, OSError) as exc:
             combined.parse_warnings.append(str(exc))
             if model is not None:
                 model.parse_warnings.append(str(exc))
 
     if model is not None:
         model.documents.documents.extend(combined.documents)
         model.documents.requirements.extend(combined.requirements)
+        model.documents.structured_requirements.extend(combined.structured_requirements)
+        model.documents.documentation_rows.extend(combined.documentation_rows)
+        model.documents.documentation_extraction_summary = _merge_summary(model.documents.documentation_extraction_summary, combined.documentation_extraction_summary)
+        model.documents.requirement_extraction_limitations.extend(combined.requirement_extraction_limitations)
         if not combined.requirements and combined.documents:
             model.parse_warnings.append(
                 "No structured requirements found across uploaded documentation set."
             )
 
     return combined
 
 
+
+
+def _merge_summary(base: Dict[str, Any], update: Dict[str, Any]) -> Dict[str, Any]:
+    merged = dict(base or {})
+    for key, value in (update or {}).items():
+        existing = merged.get(key)
+        if isinstance(value, bool):
+            merged[key] = bool(existing) or value
+        elif isinstance(value, int):
+            merged[key] = int(existing or 0) + value
+        else:
+            merged[key] = value
+    return merged
+
+
+def _extract_docx_rows(path: str) -> Dict[str, Any]:
+    from docx import Document
+    doc = Document(path)
+    rows=[]
+    reqs=[]
+    limitations=[]
+    for table in doc.tables:
+        for row in table.rows:
+            cells=[clean_raw_token(c.text) for c in row.cells]
+            cells=[c for c in cells if c]
+            if not cells:
+                continue
+            if len(cells)==1:
+                rid, txt = _split_requirement_line(cells[0])
+            else:
+                rid, txt = cells[0], ' '.join(cells[1:])
+            if _looks_like_header_row(rid, txt):
+                continue
+            norm=_normalize_requirement_id(rid)
+            rows.append({'id':norm,'text':txt})
+            if norm and txt and _is_requirement_identifier(rid):
+                reqs.append(_build_requirement_record(norm,[txt],path))
+    para_text='\n'.join(clean_raw_token(p.text) for p in doc.paragraphs if clean_raw_token(p.text))
+    para_reqs=_extract_structured_requirements_from_lines(para_text,path)
+    reqs.extend(para_reqs)
+    dedup=[]; seen=set()
+    for r in reqs:
+        k=(r.requirement_id,r.text.strip().lower())
+        if k in seen: continue
+        seen.add(k); dedup.append(r)
+    if not rows and not para_text:
+        limitations.append('DOCX text not accessible via python-docx (possible shapes/textboxes).')
+    return {'requirements':dedup,'rows':rows,'summary':{'tables_processed':len(doc.tables),'paragraph_lines':len(para_text.splitlines()),'unstructured_doc_text_detected':bool(para_text)},'limitations':limitations}
+
+
+def _split_requirement_line(line: str):
+    m=STRUCTURED_REQUIREMENT_LINE_PATTERN.match(line)
+    if not m:
+        return '', line
+    return m.group('rid'), clean_raw_token(m.group('body'))
+
+
+def _is_requirement_identifier(text: str) -> bool:
+    token = clean_raw_token(text)
+    if not token:
+        return False
+    return REQUIREMENT_ID_PATTERN.fullmatch(token) is not None
+
+
+def _looks_like_header_row(left: str, right: str) -> bool:
+    l=(left or '').strip().lower(); r=(right or '').strip().lower()
+    return l in {'identifier','id','requirement id'} and r in {'definition','description','requirement','details'}
+
 def _read_document_text(path: str) -> str:
     extension = os.path.splitext(path)[1].lower()
 
     if extension in [".txt", ".md", ".hsl", ".sub", ".stp", ".res"]:
         return normalize_hamilton_text(read_text_file(path))
 
     if extension == ".docx":
         return normalize_hamilton_text(_read_docx_text(path))
 
     if extension == ".pdf":
         return normalize_hamilton_text(_read_pdf_text(path))
 
     return normalize_hamilton_text(read_text_file(path))
 
 
 def _read_docx_text(path: str) -> str:
     try:
         from docx import Document
     except ImportError as exc:
         raise DocumentationParserError(
             "python-docx is required for .docx requirement parsing"
         ) from exc
 
     doc = Document(path)
     chunks: List[str] = []
@@ -263,52 +357,54 @@ def _build_requirements(blocks: List[str], source_file: str) -> List[Requirement
         req_id = _extract_requirement_id(block) or "REQ_{0:04d}".format(index)
 
         record = RequirementRecord(
             requirement_id=req_id,
             source_file=source_file,
             section=_extract_section(block),
             text=block,
         )
 
         _populate_requirement_fields(record)
 
         requirements.append(record)
 
     return requirements
 
 
 def _extract_requirement_id(text: str) -> Optional[str]:
     match = REQUIREMENT_ID_PATTERN.search(text or "")
     if not match:
         return None
 
     return clean_raw_token(match.group(1))
 
 
 def _normalize_requirement_id(raw_requirement_id: str) -> str:
-    value = clean_raw_token(raw_requirement_id).upper().replace(" ", "")
+    value = clean_raw_token(raw_requirement_id).upper()
     value = value.replace("_", "-")
+    value = re.sub(r"^(UR|REQ|RQ|MP|SOP)\s+(\d{1,6})$", r"\1-\2", value)
+    value = value.replace(" ", "")
     return value
 
 
 def _extract_section(text: str) -> Optional[str]:
     match = SECTION_PATTERN.search(text or "")
     if not match:
         return None
 
     value = clean_raw_token(match.group(1))
     return value if value else None
 
 
 def _populate_requirement_fields(record: RequirementRecord) -> None:
     text = record.text or ""
 
     record.expected_input_files = extract_file_references(text)
     record.expected_output_files = extract_file_references(text)
 
     record.expected_volumes_ul = extract_volume_requirements(text)
     record.expected_sample_max = extract_max_sample_requirement(text)
 
     record.expected_tip_types = _extract_tip_types(text)
     record.expected_sequences = _extract_sequences(text)
     record.expected_labware = _extract_labware(text)
 



******
TEST DOC

diff --git a/tests/test_documentation_parser.py b/tests/test_documentation_parser.py
index bdef581a72a8e133680de886f3d52e37ce0c2206..55a9bf90b4282a3755ecad16a335873b0de0be80 100644
--- a/tests/test_documentation_parser.py
+++ b/tests/test_documentation_parser.py
@@ -1,51 +1,94 @@
 from pathlib import Path
 
 from models.method_models import MethodModel
 from parsers.documentation_parser import parse_documentation_file, parse_multiple_documents
-from parsers.documentation_parser import parse_documentation_file
 
 
 FIXTURE_DIR = Path(__file__).parent / "sample_inputs"
 
 
 def test_parse_txt_requirements_records():
     document_set = parse_documentation_file(str(FIXTURE_DIR / "user_requirements.txt"))
 
     assert document_set.requirements
     assert len(document_set.requirements) >= 3
     assert document_set.requirements[0].requirement_id.startswith("UR-")
 
 
 def test_parse_md_requirements_records(tmp_path):
     md_path = tmp_path / "requirements.md"
     md_path.write_text(
         """
 # UR / MP
 UR-001: Initialize method before transfer.
 This must happen first.
 
 MP-010: Use 300uL tips for buffer sequence.
 SOP-200: Retry 2 times after recovery event.
 """.strip(),
         encoding="utf-8",
     )
 
     document_set = parse_documentation_file(str(md_path))
 
     assert len(document_set.requirements) == 3
     assert document_set.requirements[0].requirement_id == "UR-001"
     assert "Initialize method" in document_set.requirements[0].text
     assert document_set.requirements[2].requires_recovery is True
     assert document_set.requirements[2].requires_abort_after_attempts == 2
 
 
 def test_parse_multiple_documents_adds_warning_when_no_structured_requirements(tmp_path):
     model = MethodModel()
     note_path = tmp_path / "note.txt"
     note_path.write_text("General prose only. No requirement identifiers here.", encoding="utf-8")
 
     doc_set = parse_multiple_documents([str(note_path)], model=model)
 
     assert doc_set.documents
     assert not doc_set.requirements
     assert any("No structured requirements" in warning for warning in model.parse_warnings)
+
+
+def test_parse_structured_paragraph_underscore_ids(tmp_path):
+    doc_path = tmp_path / "requirements_underscore.docx"
+    from docx import Document
+
+    doc = Document()
+    doc.add_paragraph("UR_001: System must notify user.")
+    doc.add_paragraph("SOP_12: Operator must verify setup.")
+    doc.save(str(doc_path))
+
+    document_set = parse_documentation_file(str(doc_path))
+
+    ids = [req.requirement_id for req in document_set.structured_requirements]
+    assert "UR-001" in ids
+    assert "SOP-12" in ids
+
+
+def test_docx_table_rows_gate_structured_requirements_by_valid_id(tmp_path):
+    doc_path = tmp_path / "requirements_table_gate.docx"
+    from docx import Document
+
+    doc = Document()
+    table = doc.add_table(rows=4, cols=2)
+    table.rows[0].cells[0].text = "Custom Method - Demo"
+    table.rows[0].cells[1].text = ""
+    table.rows[1].cells[0].text = "Random Label"
+    table.rows[1].cells[1].text = "Some table text"
+    table.rows[2].cells[0].text = "MP_001"
+    table.rows[2].cells[1].text = "Method must verify lookup table."
+    table.rows[3].cells[0].text = "SOP 12"
+    table.rows[3].cells[1].text = "Operator must verify setup."
+    doc.save(str(doc_path))
+
+    document_set = parse_documentation_file(str(doc_path))
+    rows = document_set.documentation_rows
+    structured_ids = {req.requirement_id for req in document_set.structured_requirements}
+
+    assert any(row["text"] == "Custom Method - Demo" for row in rows)
+    assert any(row["id"] == "RANDOMLABEL" for row in rows)
+    assert "CUSTOMMETHOD-DEMO" not in structured_ids
+    assert "RANDOMLABEL" not in structured_ids
+    assert "MP-001" in structured_ids
+    assert "SOP-12" in structured_ids




******* 
** NEWWW FILEEEE ->>> test_pass6_documentation_and_summary.py (tests)




diff --git a/tests/test_pass6_documentation_and_summary.py b/tests/test_pass6_documentation_and_summary.py
new file mode 100644
index 0000000000000000000000000000000000000000..3bf6c0ce896b89cce1ec0860836a01a7cf62965c
--- /dev/null
+++ b/tests/test_pass6_documentation_and_summary.py
@@ -0,0 +1,30 @@
+from parsers.documentation_parser import _merge_summary, parse_multiple_documents
+from models.method_models import MethodModel
+
+
+def test_merge_summary_preserves_bool_and_sums_int():
+    out = _merge_summary({"unstructured_doc_text_detected": False, "rows": 1}, {"unstructured_doc_text_detected": True, "rows": 2})
+    assert out["unstructured_doc_text_detected"] is True
+    assert out["rows"] == 3
+
+
+def test_parse_multiple_documents_propagates_metadata():
+    model = MethodModel()
+    doc_set = parse_multiple_documents(["sample_inputs/user_requirements_table_example.docx"], model=model)
+    assert isinstance(doc_set.documentation_rows, list)
+    assert isinstance(doc_set.documentation_extraction_summary, dict)
+
+
+def test_pipeline_safety_booleans_false():
+    from core.config import DevPalConfig
+    from core.pipeline import run_pipeline
+    from core.report_builder import build_full_report
+
+    cfg = DevPalConfig(selected_method_path="sample_inputs/Demo.med", layout_paths=["sample_inputs/layout.csv"], document_paths=["sample_inputs/user_requirements.txt"], auto_discover_traces=False)
+    m = run_pipeline(cfg)
+    report = build_full_report(m)
+    r = report["runtime_environment"]
+    assert r["execution_performed"] is False
+    assert r["hardware_execution_occurred"] is False
+    assert r["venus_launched"] is False
+    assert r["method_execution_occurred"] is False



