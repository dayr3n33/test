python -c "import json; d=json.load(open(r'sample_inputs/devpal_reports/devpal_report.json')); print(json.dumps(d.get('sequence_resolution_summary'), indent=2)); print(json.dumps(d.get('transfer_sequence_resolution'), indent=2)[:4000]); print(json.dumps(d.get('sequence_alias_resolution_summary'), indent=2)); print(json.dumps(d.get('unresolved_sequence_mapping_blockers'), indent=2)[:4000])"
