Rejecting this edit.

This proposed change is unsafe and appears to corrupt parsers/lay_parser.py.

Problems:
1. COMPACT_VALUE_BOUNDARY_PATTERN still contains forbidden unqualified boundaries:
   ObjId
   PosId
   DefSeqValid
   File
   Template
   LwProperties

2. The edit duplicates regex fragments into SEQUENCE_NAME_STRUCTURAL_TOKEN_PATTERN and SEQUENCE_NAME_KEY_TOKEN_PATTERN incorrectly.

3. It appears to corrupt unrelated parser code and duplicate return summary.

Do not modify SEQUENCE_NAME_STRUCTURAL_TOKEN_PATTERN or SEQUENCE_NAME_KEY_TOKEN_PATTERN for this hotfix.

Minimal-change requirement:
Only change COMPACT_VALUE_BOUNDARY_PATTERN.

Replace the whole COMPACT_VALUE_BOUNDARY_PATTERN with exactly this:

COMPACT_VALUE_BOUNDARY_PATTERN = re.compile(
    r"(?i)(?="
    r"Seq\.\d+\.Name"
    r"|Seq\.\d+\.Cnt"
    r"|Seq\.\d+\.ReadOnly"
    r"|Seq\.\d+\.Item\.\d+\.ObjId"
    r"|Seq\.\d+\.Item\.\d+\.PosId"
    r"|Labware\.\d+\.Id"
    r"|Labware\.\d+\.DefSeqValid"
    r"|Labware\.\d+\.File"
    r"|Labware\.\d+\.Template"
    r"|Labware\.\d+\.SiteId"
    r"|Labware\.\d+\.LwProperties\.\d+\.Property"
    r"|Labware\.\d+\.LwProperties\.\d+\.Value"
    r"|Layer\.\d+\.\d+\.SeqName"
    r"|Layer\.\d+\.\d+\.LabwareName"
    r")"
)

Then add only the requested tests.

Do not touch unrelated parser logic.
Do not modify unrelated regex constants.
Do not duplicate existing code.
Do not add standalone ObjId, PosId, File, Template, DefSeqValid, or LwProperties boundaries.
