if (
    item.get("autoincrement")
    and item.get("action") in {"aspirate", "dispense"}
    and not bool(item.get("manual"))
    and seq
    and increment > 0
):
    review_lines.append(
        f"ph_inc_sequence(ham_int, sequence='{seq}', increment={increment})"
    )
