PYHAMILTON_IMPORT_ERROR = ""

try:
    import pyhamilton.interface as phi

    from pyhamilton import (
        HamiltonError,
        HamiltonStepError,
        HamiltonTimeoutError,
        LayoutManager,
        Plate96,
        ResourceType,
        OEM_HSL_PATH,
        OEM_RUN_EXE_PATH,
        normal_logging,
        initialize,
        tip_pick_up_seq,
        aspirate_seq,
        dispense_seq,
        tip_pick_up_96_seq,
        aspirate_96_seq,
        dispense_96_seq2,
        tip_eject_96,
    )

    try:
        from pyhamilton import inc_sequence
    except ImportError:
        try:
            from pyhamilton import increment_sequence as inc_sequence
        except ImportError:
            def inc_sequence(ham_int, sequence, increment):
                return ham_int.send_command(
                    command="INCSEQ",
                    sequence=sequence,
                    increment=increment,
                )

    try:
        from pyhamilton import tip_eject_seq2
    except ImportError:
        try:
            from pyhamilton import tip_eject_seq as tip_eject_seq2
        except ImportError:
            from pyhamilton import tip_eject as tip_eject_seq2

except Exception as exc:
    PYHAMILTON_IMPORT_ERROR = f"{type(exc).__name__}: {exc}"

    phi = None
    HamiltonStepError = Exception
    HamiltonError = Exception
    HamiltonTimeoutError = Exception
    LayoutManager = None
    Plate96 = None
    ResourceType = None
    OEM_RUN_EXE_PATH = ""
    OEM_HSL_PATH = ""
