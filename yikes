
Do not modify:

SEQUENCE_NAME_STRUCTURAL_TOKEN_PATTERN
SEQUENCE_NAME_KEY_TOKEN_PATTERN
PUNCTUATION_BOUNDARY_FRAGMENT_PATTERN
Any sequence validation logic

Modify ONLY:

COMPACT_VALUE_BOUNDARY_PATTERN

The previous edit changed additional parser validation patterns. Reject those changes and generate a new patch that only updates COMPACT_VALUE_BOUNDARY_PATTERN.

The only goal is removing standalone boundaries:

ObjId(?:\b|_)
PosId(?:\b|_)
File(?:\b|_)
Template(?:\b|_)
DefSeqValid(?:\b|_)
LwProperties(?:\b|_)

and replacing them with qualified serialized-key boundaries.

Show only the lay_parser.py diff before making changes.
