python - <<'PY'
import json
from pathlib import Path

p = Path(r"C:\Users\lynchd27\OneDrive - Pfizer\Documents\PycharmProjects\pyhamilton\new-project\sample_inputs\devpal_reports\devpal_report.json")
data = json.loads(p.read_text(encoding="utf-8"))

for key in [
    "mlstar_command_calls",
    "mlstar_command_argument_resolution",
    "transfer_command_argument_resolution",
    "transfer_sequence_resolution",
    "mlstar_argument_object_graphs",
    "mlstar_argument_field_inventory",
    "expected_transfer_model",
]:
    print(f"\n\n===== {key} =====")
    print(json.dumps(data.get(key), indent=2)[:6000])
PY
